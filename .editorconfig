root = true

[*]
end_of_line = lf
insert_final_newline = true
charset = utf-8
indent_style = space
indent_size = 2
tab_width = 4
trim_trailing_whitespace = true

[*.{kt, kts}]
disabled_rules = import-ordering, filename, colon-spacing, comment-spacing, no-multi-spaces, max_line_length
